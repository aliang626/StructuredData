# ARM架构优化的依赖文件 - 适用于麒麟系统
Flask==2.3.3
Flask-CORS==4.0.0
Flask-SQLAlchemy==3.0.5
Flask-Migrate==4.0.5
SQLAlchemy==2.0.21
pymysql==1.1.0

# ARM优化：使用非binary版本，支持ARM编译
psycopg2>=2.9.5  # 改为非binary版本，支持ARM
cryptography>=41.0.0  # 使用>=以获得更好的ARM支持

# 科学计算库 - ARM版本
numpy>=1.24.0,<1.25.0  # ARM支持较好的版本范围
scipy>=1.10.0,<1.12.0
pandas>=2.0.0,<2.1.0

# 机器学习 - ARM优化
scikit-learn>=1.3.0,<1.4.0  # 允许更灵活的版本选择
xgboost>=1.7.0,<2.0.0  # ARM支持的版本范围

# 可视化
matplotlib>=3.7.0,<3.8.0
seaborn>=0.12.0,<0.13.0

# 其他工具
configparser==6.0.0
python-dotenv==1.0.0
Werkzeug==2.3.7
openpyxl==3.1.2
requests==2.31.0
aiohttp==3.8.5

# ARM系统可能需要的额外依赖
wheel>=0.40.0  # 确保wheel支持
setuptools>=65.0.0  # 编译支持
